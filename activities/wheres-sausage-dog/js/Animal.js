class Animal {
    constructor() {
        this.x = x;
        this.y = y;
        this.image = image;
        this.angle = 0;
        
    }



    update() {
        display();x``
    }
    
    display() {
        push();
        translate(this.x,this.y);
        rotate(this.angle)
        imageMode(CENTER);
        image
        pop();
    }
}